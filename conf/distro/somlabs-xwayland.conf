require conf/distro/include/fsl-base.inc

DISTRO = "somlabs-xwayland"
DISTRO_NAME = "SoMLabs Wayland with XWayland"

INIT_MANAGER = "systemd"

DISTRO_FEATURES:remove = "directfb nfs"
DISTRO_FEATURES:append = " x11 wayland pam"

